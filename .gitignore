__pycache__/
*.py[cod]
.pytest_cache/
.venv/
dist/
build/
*.egg-info/
# runtime artifacts — never commit identities or witness logs
*.keystore
*.jsonl
salt_vault*
# scratch for scripts/typeset_roadmap.py — never shipped, and excluded
# from source_digest() so its presence cannot invalidate a recorded run
.typeset/
