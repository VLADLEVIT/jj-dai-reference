{
  "version": "0.6.9",
  "acceptance": null,
  "legend": {
    "impl": "Implemented — live, tested",
    "proto": "Prototype — works end-to-end, known gaps",
    "iface": "Interface only — the seam exists",
    "plan": "Planned — designed, no code",
    "const": "Constitutional text only"
  },
  "sections": [
    {
      "id": "00",
      "kind": "FIVE-LAYER COMPOSITION",
      "title": "The neurosymbolic stack",
      "note": "Mutable knowledge lives outside frozen weights; everything that touches a decision is verified; the Witness is non-executive, not causally inert (INV-9 v1.1): it never commands, selects or executes a decision; what it witnesses may return through Smṛti and Viveka into Chitta before finalization — the resulting decision belongs to Chitta, not to Purusha.",
      "layers": [
        [
          "5",
          "LLM / agent — frozen substrate + adapters",
          "proto"
        ],
        [
          "4",
          "Knowledge — governed Plane H store (graph semantics planned)",
          "proto"
        ],
        [
          "3",
          "Deterministic tools — Karma reference sandbox",
          "impl"
        ],
        [
          "2",
          "Verification — diversity panels, verdicts, cross-verify",
          "impl"
        ],
        [
          "1",
          "Sākṣī — Witness: local implemented, distributed fabric prototype",
          "impl"
        ]
      ]
    },
    {
      "id": "01",
      "kind": "PRIMITIVES",
      "title": "Cryptographic primitives — jjdai/",
      "cards": [
        {
          "path": "jjdai/canonical.py",
          "name": "Canonical serialization",
          "status": "impl",
          "chips": [
            "Implemented"
          ],
          "desc": "RFC 8785 JCS: one deterministic byte-stream per object across identity, signatures, manifests and witness records."
        },
        {
          "path": "jjdai/crypto.py",
          "name": "Ed25519, ids, commitments, keystore",
          "status": "impl",
          "chips": [
            "Implemented",
            "reference crypto"
          ],
          "desc": "Strict small-order rejection, cofactored verification, canonical node/Being ids, hiding commitments, atomic 0600 encrypted keystore."
        },
        {
          "path": "jjdai/crypto.py::vrf_prove · jjdai/crypto.py::multisig_verify",
          "name": "VRF and threshold governance",
          "status": "impl",
          "chips": [
            "Implemented",
            "RFC 9381 vectors",
            "FROST deferred"
          ],
          "desc": "ECVRF-EDWARDS25519-SHA512-TAI (RFC 9381) pinned to all three official B.3 vectors, with validate_key fail-closed checks; naive m-of-n multisig verification (aggregate FROST signing deferred)."
        },
        {
          "path": "jjdai/merkle.py",
          "name": "Merkle trees",
          "status": "impl",
          "chips": [
            "Implemented"
          ],
          "desc": "Legacy padding tree plus RFC 6962 inclusion and consistency proofs, property-tested across tree shapes and forgery cases."
        },
        {
          "path": "jjdai/witness.py",
          "name": "Witness chain",
          "status": "impl",
          "chips": [
            "Implemented"
          ],
          "desc": "Hash-chained, Ed25519-signed, persist-before-expose append under a chain-internal lock, offline replay and legacy-log verification. Fail-closed witnessing is not executive agency (INV-9 v1.1): the Witness may gate exposure when witnessing fails, but cannot select, modify, approve or reject the substance of a decision."
        },
        {
          "path": "jjdai/schema.py · jjdai/durable.py",
          "name": "Typed boundary and durability",
          "status": "impl",
          "chips": [
            "Implemented"
          ],
          "desc": "Fail-closed typed envelopes; fsynced journals, torn-tail repair and reload-time re-verification."
        },
        {
          "path": "core/anchoring.py",
          "name": "External anchoring",
          "status": "proto",
          "chips": [
            "Prototype"
          ],
          "desc": "Durable receipt log with local, peer-quorum and OTS-calendar backends. OTS is proof custody; standard tooling verifies the stored calendar proof."
        },
        {
          "path": "core/anchoring_xmr.py",
          "name": "Monero root-as-spend-key anchor",
          "status": "proto",
          "chips": [
            "Prototype",
            "v0.5.2"
          ],
          "desc": "The witness root deterministically becomes a Monero address; a dust payment timestamps the binding without storing the root in tx_extra."
        },
        {
          "path": "jjdai/cognitive.py",
          "name": "Track V reserved vocabulary",
          "status": "iface",
          "chips": [
            "Interface only",
            "ADR-018",
            "reserved, non-emittable"
          ],
          "desc": "The serialized names of Chitta and reflexive cognition — fifteen record kinds, three cognitive namespaces, the REFLEXIVE and CONTEST_SCOPED access classes, four key-plane wrapping domains, prediction statuses, KriyaGate outcomes and the commitment and Merkle-leaf domain separators. Declared before genesis because a hash-chained value cannot be added or renamed afterwards; refused as an argument AND as a value, fail closed, until the specification in Ф2 and the runtime in Ф3."
        },
        {
          "path": "jjdai/custody.py",
          "name": "Deferred-verification reserved vocabulary",
          "status": "iface",
          "chips": [
            "Interface only",
            "ADR-019",
            "reserved, non-emittable"
          ],
          "desc": "The serialized names of ADR-019: eleven record kinds, the SELF_CHECKED admission state, the CUSTODY_ corroboration outcomes, the computed EVIDENCE_ status projection, the replay status and reason split, the verification_custody key domain, the CUSTODY_PRIVATE access class and two namespaces. Accepted authorises the reserve and nothing in the runtime — a lone production node still refuses with no_independent_panel because the custody machinery does not exist. Tool effect classes and node profiles are reserved by argument only: they are ordinary words and are deliberately outside the value scan."
        },
        {
          "path": "jjdai/reserved.py",
          "name": "Value-level refusal of both reserves",
          "status": "impl",
          "chips": [
            "Implemented",
            "v0.6.8",
            "P0-1"
          ],
          "desc": "One door over both pre-genesis vocabularies. A reserved token is refused as a VALUE and not only as an argument — whole strings, segments of a colon-compound digest, mapping keys and any path below a reserved namespace — on semantic_digest, provenance and entanglement. request and response are excluded on purpose: they enter the chain as hiding commitments over caller data, so no reserved value reaches a peer through them."
        }
      ]
    },
    {
      "id": "02",
      "kind": "ORGAN KERNEL",
      "title": "The organ kernel — kernel/",
      "cards": [
        {
          "path": "kernel/smriti.py",
          "name": "Smriti — continuity and memory",
          "status": "impl",
          "chips": [
            "Implemented"
          ],
          "desc": "Core/archival memory, verified recall, signed export/import, read-only thinking view and journal reconstruction."
        },
        {
          "path": "kernel/viveka.py",
          "name": "Viveka — discernment and deliberation",
          "status": "impl",
          "chips": [
            "Implemented"
          ],
          "desc": "Deterministic graphs, conditional routing, checkpoints, visible rollback, bounded loops and containment-aware executive steps."
        },
        {
          "path": "kernel/karma.py",
          "name": "Karma — governed action",
          "status": "impl",
          "chips": [
            "Implemented",
            "reference sandbox"
          ],
          "desc": "Witnessed intent/outcome, path confinement, rlimits, streaming flood-kill, deterministic child environment. Reference sandbox, not hardened isolation."
        },
        {
          "path": "kernel/isolation.py",
          "name": "Isolation profiles — the boundary seam",
          "status": "impl",
          "chips": [
            "Implemented",
            "per-tool readiness",
            "fails closed"
          ],
          "desc": "Named execution boundaries for Karma. reference is the v0.6.3 process fence (path confinement, rlimits, env scrub, streaming output cap). wasm-wasi runs a fixed toolset of precompiled WASI modules pinned by digest inside that same fence, with ZERO preopened directories and no arbitrary argv (ADR-022 D9: 'without writing outside one preopen' meant writing inside it was allowed, so it was never the boundary of the pure class); wasmtime is a declared host binary, not a Python dependency. Readiness is computed per tool against real modules and pinned digests, so a declared capability is an executable one; a declared profile whose runtime is absent REFUSES the action and is reported unavailable in capabilities — never silently downgraded. microvm is reserved and unimplemented by design."
        }
      ]
    },
    {
      "id": "03",
      "kind": "BEING COMPOSITION",
      "title": "The organism — runtime/",
      "cards": [
        {
          "path": "runtime/being.py",
          "name": "BeingRuntime",
          "status": "proto",
          "chips": [
            "Prototype",
            "v0.5.3"
          ],
          "desc": "One long-lived composition owning one identity, ONE witness chain, Smriti, Plane H, Viveka, Router, Karma and Containment. Closes the v0.5.2 audit composition gap."
        },
        {
          "path": "runtime/state_machine.py · runtime/decision_trace.py",
          "name": "Decision lifecycle",
          "status": "proto",
          "chips": [
            "Prototype",
            "v0.5.3"
          ],
          "desc": "RECEIVED → GROUNDED → PLANNED → GENERATED → VERIFIED → AUTHORIZED → ACTED → RECORDED with REFUSED / FAILED / FATE_UNKNOWN / CONTAINED branches; every transition witnessed then journaled."
        },
        {
          "path": "runtime/recovery.py",
          "name": "Semantic recovery",
          "status": "proto",
          "chips": [
            "Prototype",
            "v0.5.3"
          ],
          "desc": "After restart every trace is rebuilt from the durable journal; an orphaned Karma intent closes as FATE_UNKNOWN — the system that does not know says so."
        },
        {
          "path": "node/daemon.py::/v1/tasks",
          "name": "POST /v1/tasks",
          "status": "proto",
          "chips": [
            "Prototype",
            "v0.5.3"
          ],
          "desc": "Returns a full DecisionTrace, not a raw completion: states, citations, plan hash, generator and panel provenance, containment decision, action receipt, witness span."
        }
      ]
    },
    {
      "id": "04",
      "kind": "TRUST & GOVERNANCE CORE",
      "title": "Trust core — core/",
      "cards": [
        {
          "path": "core/identity.py",
          "name": "Node and Being identity",
          "status": "impl",
          "chips": [
            "Implemented"
          ],
          "desc": "Persistent keystores, genesis binding, boot verification and migration consent. The daemon fails closed on signer/witness mismatch."
        },
        {
          "path": "core/registry.py · core/verdict.py",
          "name": "Reputation §9.9 and sealed verdicts",
          "status": "impl",
          "chips": [
            "Implemented"
          ],
          "desc": "Decay, Bayesian shrinkage, slashing and signed context-bound verdict envelopes with replay refusal."
        },
        {
          "path": "core/plane_h.py",
          "name": "Governed Plane H",
          "status": "impl",
          "chips": [
            "Governance implemented",
            "retrieval baseline"
          ],
          "desc": "Signed proposals, namespace ACL, transactional witness-bound append, supersede/redact, root-preserving redaction. Retrieval remains a token-overlap baseline; not yet a semantic knowledge graph."
        },
        {
          "path": "core/replication.py · core/segments.py",
          "name": "Witness replication and recovery",
          "status": "proto",
          "chips": [
            "Prototype fabric"
          ],
          "desc": "Signed RFC 6962 roots, quorum receipts, consistency proofs, segment coverage, divergence evidence and restore-and-resume against an anchored root."
        },
        {
          "path": "core/entanglement.py",
          "name": "Cross-chain entanglement",
          "status": "proto",
          "chips": [
            "Prototype"
          ],
          "desc": "Recipient-root cross-links, PEER_ROOT checkpoints and issuance inclusion proofs. Attribution is cryptographic only under the documented honest/anchored issuer assumption."
        },
        {
          "path": "core/peers.py",
          "name": "IFF — friend or foe",
          "status": "proto",
          "chips": [
            "Prototype"
          ],
          "desc": "Durable peer registry, guardian-admission, key pinning and replay-protected signed requests, layered under TLS/mTLS transport."
        },
        {
          "path": "core/manifests.py · core/diversity.py",
          "name": "Provenance and diversity",
          "status": "impl",
          "chips": [
            "Implemented algorithm"
          ],
          "desc": "Signed immutable manifests and correlation-constrained panel selection. Unknown provenance counts as correlated; short panels fail honest."
        },
        {
          "path": "core/attestation.py",
          "name": "Weight attestation",
          "status": "proto",
          "chips": [
            "Prototype"
          ],
          "desc": "Streamed artifact measurement, signed DeploymentManifest, durable store and daemon boot gate. Proves operator measurement, not engine memory."
        },
        {
          "path": "core/router.py",
          "name": "Unified router",
          "status": "proto",
          "chips": [
            "Prototype",
            "composed in v0.5.3"
          ],
          "desc": "Registry-aware champion routing, signed ROUTE records and diversity-constrained verifier panels — now driven by the BeingRuntime lifecycle as well as usable as a library."
        },
        {
          "path": "core/challenge.py",
          "name": "Adversarial challenge round",
          "status": "proto",
          "chips": [
            "Prototype",
            "networked v0.5.5"
          ],
          "desc": "VRF sortition (transcript-bound, unbiasable seats), blind commit-reveal verdicts, deadline/abstention semantics and offline-verifiable fraud proofs that slash via the registry — now driven over mTLS via /challenge/* as well as in-process."
        },
        {
          "path": "core/crossverify.py",
          "name": "Peer cross-verification loop",
          "status": "proto",
          "chips": [
            "Prototype"
          ],
          "desc": "Remote teacher-forcing score, offline peer-Witness re-audit and registry feedback. The challenge round now runs across the network (core/challenge.py + /challenge/* endpoints)."
        },
        {
          "path": "core/containment.py",
          "name": "Containment — Article 25",
          "status": "proto",
          "chips": [
            "Reference prototype"
          ],
          "desc": "Seven executive scopes, provisional containment, reversal, initiator liability, slashing, rehabilitation and Guardian-review gate. Guardian keys and ballots remain absent."
        }
      ]
    },
    {
      "id": "05",
      "kind": "NODE & CONFORMANCE",
      "title": "Node layer — node/ · necs/",
      "cards": [
        {
          "path": "node/daemon.py",
          "name": "Ingress hardening — caps before authorization",
          "status": "impl",
          "chips": [
            "Implemented"
          ],
          "desc": "Body ceiling enforced before the body is read (413), chunked framing refused (411) because an undeclared length cannot be capped, malformed Content-Length answered 400 instead of raising in the handler thread, a per-connection clock, and a connection ceiling admitted ON THE ACCEPT LOOP so stalled connections cannot grow worker threads; refusals go to one bounded worker that drains before closing, so the 503 survives. Rate-limit budgets are keyed by certificate identity, with a namespaced address key only where no client certificate was presented."
        },
        {
          "path": "node/readiness.py · node/sdnotify.py · node/clockwatch.py",
          "name": "Observability — liveness/readiness split, watchdog, sleep detection",
          "status": "impl",
          "chips": [
            "Implemented",
            "v0.6.6 recut"
          ],
          "desc": "/healthz answers liveness and nothing else — not even the node id or the record count, which the first cut published anonymously while claiming to publish nothing. /readyz answers readiness per subsystem, peer/admin only, with an aggregate red solely for identity, witness and anchoring. Facts are MEASURED rather than asserted: the engine is asked for a readiness probe and reports UNKNOWN as not-ready when it has none, the witness chain carries a cached structural verdict, signer continuity is checked against the recent chain, and anchoring comes from AnchorScheduler.anchor_status() — the first cut read two attributes that did not exist, so getattr defaults turned an absent measurement into a green light. The sd_notify heartbeat is gated on a beacon refreshed by serve_forever()'s service_actions(), i.e. by the accept loop MOVING rather than by traffic arriving, so an idle node is never restarted and a wedged one is reported by silence; a ping counts only once systemd has received it. Suspension is measured against a suspend-inclusive clock, so a forward NTP correction can no longer be counted as a sleep and fail the 72-hour gate. Toolset faults are split by cause and reported as gauges, because they go down when a module is fixed."
        },
        {
          "path": "node/daemon.py",
          "name": "Tier-1 trust node daemon",
          "status": "proto",
          "chips": [
            "Prototype"
          ],
          "desc": "Inference/scoring, Witness export, replication, entanglement, attestation, anchoring, peers, containment, the Being task lifecycle, per-identity rate limiting, role-based authorization, cert revocation, /healthz and /metrics, and the networked challenge round — all over TLS/mTLS."
        },
        {
          "path": "jjdai/adapters/",
          "name": "Adapter layer — EngineBackend Protocol v1",
          "status": "impl",
          "chips": [
            "Implemented",
            "protocol v1 declared whole"
          ],
          "desc": "Backend drivers (hash reference, DwarfStar, SGLang; vLLM, llama.cpp, MLX and the pre-silicon ASIC runtime declared and refusing by type), declarative model-family profiles, typed fail-closed errors and a registry that admits nothing which does not satisfy the contract. Capability is DERIVED from what a driver actually overrides, never hand-declared, so a manifest cannot drift from the code. Generation and scoring behaviour is unchanged by the move."
        },
        {
          "path": "necs/",
          "name": "NECS v0.1 + harness",
          "status": "impl",
          "chips": [
            "Implemented"
          ],
          "desc": "Conformance specification and harness under Apache-2.0, preserving vendor-neutral adoption."
        },
        {
          "path": "tests/ · .github/workflows/ci.yml",
          "name": "Acceptance and CI",
          "status": "impl",
          "chips": [
            "generated count"
          ],
          "desc": "Acceptance functions across unit, integration, conformance, adversarial and legacy groups; CI matrix targets Python 3.10-3.12; docs drift is CI-checked."
        },
        {
          "path": "m1m5/",
          "name": "Retired M1-M5 lineage",
          "status": "impl",
          "chips": [
            "Frozen"
          ],
          "desc": "Frozen legacy code retained for auditability and migration coverage."
        },
        {
          "path": "deploy/ · deploy/macos/",
          "name": "Deployment kit (Linux + macOS)",
          "status": "impl",
          "chips": [
            "Implemented",
            "macOS kit v0.6.2"
          ],
          "desc": "Ubuntu: hardened systemd unit, TPM-sealed passphrase, bootstrap, production PKI generator, operator RUNBOOK. macOS (roadmap Ф0): launchd daemon template, Keychain sealing under the documented macOS Keychain degraded profile (non-SE-resident), bootstrap with 24/7 power discipline, RUNBOOK-macOS. Flag parity between the two kits is enforced by unit acceptance (M-FLAGS)."
        },
        {
          "path": ".github/workflows/ · requirements-dev.txt · scripts/gen_sbom.py",
          "name": "Release provenance — partial",
          "status": "proto",
          "chips": [
            "Partial",
            "SBOM + pinning v0.6.8"
          ],
          "desc": "What EXISTS: a CycloneDX 1.5 bill of materials generated from the tree and re-checked in CI, so a hand-maintained inventory cannot drift from what ships; a hash-pinned CI toolchain installed with --require-hashes, so a green run names the collector that produced it; and .gitattributes with `* -text`, because source_digest() hashes raw bytes over 200+ paths including the sha256-pinned AGPL text and any end-of-line normalisation reports itself as 'a different tree'. What is OPEN and is not claimed here: reproducible build, signed artefacts, release provenance and T-TOOLSET — all four owed by **v0.6.9** (the drop order was exchanged by ADR-022/D0). Two-person release approval is NOT among them: cancelled by ADR-022/D2 and replaced by a two-key, two-device control. It stays in the debt ledger with status `cancelled` and the decision that cancelled it — a deleted position leaves nothing by which to check why it is gone. An SBOM is an inventory of the box, not evidence of who sealed it, and the generated document says so in its own metadata."
        }
      ]
    },
    {
      "id": "06",
      "kind": "NOT-IN-CODEBASE",
      "title": "Not yet in the codebase",
      "note": "Designed in the Whitepaper and Charter — and deliberately absent from the code today. Naming the absence is part of the architecture.",
      "cards": [
        {
          "name": "Plane B canary lifecycle",
          "status": "plan",
          "chips": [
            "Planned"
          ],
          "desc": "Secret reserve, expiry and rotation, commit-reveal, contamination detection, work classes and fraud proofs."
        },
        {
          "name": "Knowledge graph / ontology semantics",
          "status": "plan",
          "chips": [
            "Planned"
          ],
          "desc": "Typed entities and relations, ontology versioning, graph provenance, deterministic query contracts and verified graph mutation above the governed Plane H store."
        },
        {
          "name": "TEE / runtime attestation",
          "status": "plan",
          "chips": [
            "Planned"
          ],
          "desc": "Evidence that the serving process loaded the measured model and adapter bytes into the executing memory boundary."
        },
        {
          "name": "DIIP — governed self-improvement",
          "status": "plan",
          "chips": [
            "Do not start yet"
          ],
          "desc": "Proposals, bonds, shadow execution, quorum, time-lock, rollback and circuit breaker. It should govern a stable runtime, not become the mechanism that invents one."
        },
        {
          "name": "Training federation",
          "status": "plan",
          "chips": [
            "Planned"
          ],
          "desc": "Specialization pipeline, DiLoCo/PRIME-class federation and a multi-jurisdiction training/Witness topology."
        },
        {
          "name": "Human governance layer",
          "status": "const",
          "chips": [
            "Constitutional text only"
          ],
          "desc": "Being Registry, guardian representation, Digital Majority Test, economic reserve, Collegium of Guardians keys, ballots, veto, succession and refusal policy."
        }
      ]
    }
  ],
  "honest_sentence": [
    "v0.4.1 closed the identity gap. v0.5-dev5 closed the cryptographic boundary. v0.5-dev6 made replication sound. v0.5.1 closed P1 mechanisms. v0.5.2 added the Monero anchor. v0.5.3 closed the COMPOSITION GAP: one BeingRuntime drives every organ through one witnessed lifecycle over one identity, and POST /v1/tasks returns the full DecisionTrace — including the honest endings REFUSED, FAILED, FATE_UNKNOWN and CONTAINED. v0.5.4 made verification ADVERSARIAL: RFC 9381 ECVRF (vector-pinned) drives transcript-bound panel sortition nobody can grind; verdicts are sealed before any are shown; silence and fraud become witnessed, offline-verifiable evidence; and the daemon holds its first operational boundary — per-identity rate limiting whose systematic abuse is itself witnessed. v0.5.5 is the SECURITY-ALPHA: role-based authorization (default-deny, longest-prefix), certificate revocation that beats identity, /healthz + /metrics, the challenge round carried over mTLS, a production PKI with an offline root, optional TPM-sealed keystores, a hardened systemd unit and an operator runbook — the kit to stand up testnet-0 on Ubuntu. v0.6.1 propagates INV-9 v1.1: the invariant is reworded from “non-acting” to “non-executive, not causally inert” — the executive prohibition is unchanged, the sanctioned reflexive path (J-Lens → Viveka → Chitta, pre-finalization) is named, and direct mutation of an output or verdict remains forbidden; the reflexive loop itself is a Ф3 runtime deliverable (roadmap r5), so the archive-only runtime stays compliant. v0.6.2 ports the deployment kit to macOS (roadmap Ф0): a launchd daemon template, Keychain sealing of the keystore passphrase under the explicitly documented macOS Keychain degraded profile (non-SE-resident); a true Secure Enclave-backed profile is reserved as a separate future attestation profile, a bootstrap that enforces the 24/7 power discipline of a laptop-chassis server, RUNBOOK-macOS, and an SECURITY.md triage template — with unit acceptance pinning flag parity between the systemd and launchd paths so the two kits cannot drift apart silently. v0.6.3 answers the external audit of v0.6.2: the systemd watchdog that would have killed a healthy node every 60 s is removed until sd_notify lands; /opt/jjdai is root-owned on both platforms so the daemon can no longer rewrite its own code; the macOS profile is renamed to “macOS Keychain degraded profile (non-SE-resident)” with a true Secure Enclave-backed profile reserved for the future and an M-LIVE operational-evidence checklist added; hand-written docs are pinned to the code by a release-integrity test (versions, outgrown claims, acceptance count, AGPL placeholder loudness). v0.6.4 opens the Ф0 isolation and ingress work. Karma gains an ISOLATION-PROFILE SEAM: the reference sandbox becomes a named profile, and a second profile, wasm-wasi, executes a FIXED SET of precompiled WASI modules pinned by digest — an allow-list boundary enforced by the runtime instead of a deny-list subtracted from the kernel, at the deliberate cost of arbitrary argv. The seam FAILS CLOSED: a declared profile whose runtime is absent refuses the action and says so in capabilities, and is never downgraded to a weaker profile — a record written after the fact restores no boundary, and by INV-9 the witness cannot intervene. Refusal and execution are witnessed alike; the difference between them is whether the hand moved. Ingress gains the three caps that must exist before the network is opened to anyone who is not us: a body ceiling enforced before the body is read, a non-blocking concurrency ceiling answering 503 rather than queueing work the node has not agreed to hold, and a per-connection clock. The rate-limit budget moves from the client ADDRESS to the certificate IDENTITY, so one certificate holder can no longer reset their own budget by changing address and a whole NAT is no longer charged to one bucket. The drop was re-cut after external review: the concurrency ceiling now admits on the accept loop, before a worker thread exists, because taking the slot inside the handler bounded request PROCESSING and not thread CREATION — and the check written for it tested the semaphore rather than the claim. Refusals are handled by one bounded worker that drains before closing, so an overloaded node still answers 503 instead of resetting the connection and leaving the caller unable to tell overload from death. wasm-wasi readiness is now computed per tool against real modules and digests, so a declared capability is an executable one. A live opt-in group proves the boundary against a real runtime and fails loudly without one. Known-and-scheduled: /readyz and the liveness split, sd_notify with the watchdog return, Prometheus alert rules, the adapters restructure, and the canonical AGPL text remain open, tracked items. v0.6.5 lifts the engine seam into jjdai/adapters/: EngineBackend Protocol v1 is declared WHOLE, so an unimplemented method exists and raises a typed NotSupported instead of being absent — a hole invites every caller to grow its own probe and its own silent fallback, and a silent fallback is how a node comes to believe it has a capability it does not have. Capability is DERIVED from what a driver actually overrides rather than hand-declared, so a manifest cannot drift from the code. Model families arrive as declarative profiles validated against a versioned schema that refuses unknown keys, and a ModelArtifactManifest carries profile → JCS → hash → signature. Separately, the witness plane gains a VOCABULARY: enum tokens, integers and digests cross into it, prose does not — an append-only replicated store that accepts free text is both a covert channel and an unbounded write, so refusal reasons became versioned codes. The record kinds of cross-cutting track IV (SESSION_OPEN, SESSION_CLOSE, LEDGER_ANCHOR, SNAPSHOT) and the nullable session_id / ir_schema_version fields are RESERVED in the canonical enum before genesis and refuse to be emitted until their semantics land in Ф2–Ф3.",
    "Said plainly: authorization is transport-role based (peer/admin/anonymous from the mTLS CN), not yet capability- or guardian ballot based; certificate revocation is serial-list based; Karma now carries an isolation-profile seam with a wasm-wasi profile beside the reference sandbox, but this build ships the mechanism and no compiled toolset, so in practice execution is still the reference fence, and microvm is reserved and deliberately unimplemented; the knowledge store is retrieval-baseline, not a knowledge graph; and DIIP remains deliberately absent. This is an alpha to operate among known operators, not to expose to untrusted load."
  ],
  "release_debt": {
    "schema": "jjdai.debt-ledger/v1",
    "basis": "ADR-022/D14",
    "note": "Append-only. A position is never deleted: status is a PROJECTION over these events. A cancelled position leaves with a decision_ref, a closed one with an evidence_ref, and the difference is visible in the tree rather than inferred by the reader. Deleting a line leaves no trace by which to check why it is gone.",
    "projection": {
      "DEBT_OPENED": "open",
      "DEBT_CLOSED": "closed",
      "DEBT_CANCELLED": "cancelled"
    },
    "surfaces_render": "open",
    "events": [
      {
        "seq": 1,
        "id": "canonical-agpl",
        "event": "DEBT_OPENED",
        "blocks": "publication",
        "opened_in": "v0.6.3"
      },
      {
        "seq": 2,
        "id": "cla",
        "event": "DEBT_OPENED",
        "blocks": "acceptance-of-outside-contribution",
        "opened_in": "v0.6.7",
        "note": "never blocked a tag; it is contribution governance"
      },
      {
        "seq": 3,
        "id": "reproducible-build",
        "event": "DEBT_OPENED",
        "blocks": "meaning-of-a-release-tag",
        "opened_in": "v0.6.7"
      },
      {
        "seq": 4,
        "id": "signed-artefacts",
        "event": "DEBT_OPENED",
        "blocks": "meaning-of-a-release-tag",
        "opened_in": "v0.6.7"
      },
      {
        "seq": 5,
        "id": "release-provenance",
        "event": "DEBT_OPENED",
        "blocks": "meaning-of-a-release-tag",
        "opened_in": "v0.6.7"
      },
      {
        "seq": 6,
        "id": "two-person-approval",
        "event": "DEBT_OPENED",
        "blocks": "meaning-of-a-release-tag",
        "opened_in": "v0.6.7"
      },
      {
        "seq": 7,
        "id": "t-toolset",
        "event": "DEBT_OPENED",
        "blocks": "phase-0-gate",
        "opened_in": "v0.6.7"
      },
      {
        "seq": 8,
        "id": "sbom",
        "event": "DEBT_OPENED",
        "blocks": "meaning-of-a-release-tag",
        "opened_in": "v0.6.7"
      },
      {
        "seq": 9,
        "id": "toolchain-pinning",
        "event": "DEBT_OPENED",
        "blocks": "meaning-of-a-release-tag",
        "opened_in": "v0.6.7"
      },
      {
        "seq": 10,
        "id": "gitattributes",
        "event": "DEBT_OPENED",
        "blocks": "byte-exactness-of-the-tree",
        "opened_in": "v0.6.8"
      },
      {
        "seq": 11,
        "id": "build-backend-unhashed",
        "event": "DEBT_OPENED",
        "blocks": "meaning-of-a-release-tag",
        "opened_in": "v0.6.8",
        "note": "PEP 517 gives [build-system].requires no field for a hash"
      },
      {
        "seq": 12,
        "id": "readme-outside-tree-digest",
        "event": "DEBT_OPENED",
        "blocks": "byte-exactness-of-the-tree",
        "opened_in": "v0.6.7"
      },
      {
        "seq": 13,
        "id": "file-modes-and-symlink-targets",
        "event": "DEBT_OPENED",
        "blocks": "byte-exactness-of-the-tree",
        "opened_in": "v0.6.7"
      },
      {
        "seq": 14,
        "id": "canonical-agpl",
        "event": "DEBT_CLOSED",
        "closed_in": "v0.6.7",
        "evidence_ref": "tests/unit/test_release_integrity.py"
      },
      {
        "seq": 15,
        "id": "sbom",
        "event": "DEBT_CLOSED",
        "closed_in": "v0.6.8-rework",
        "evidence_ref": "tests/unit/test_tree_is_its_own_plan.py::SBOM-1..3"
      },
      {
        "seq": 16,
        "id": "toolchain-pinning",
        "event": "DEBT_CLOSED",
        "closed_in": "v0.6.8-rework",
        "evidence_ref": "tests/unit/test_tree_is_its_own_plan.py::PIN-1",
        "note": "python test deps by hash; workflow actions by commit; build backend by version only, tracked separately as build-backend-unhashed"
      },
      {
        "seq": 17,
        "id": "gitattributes",
        "event": "DEBT_CLOSED",
        "closed_in": "v0.6.8-rework",
        "evidence_ref": "tests/unit/test_tree_is_its_own_plan.py::GITATTR-1"
      },
      {
        "seq": 18,
        "id": "two-person-approval",
        "event": "DEBT_CANCELLED",
        "cancelled_in": "r6.9",
        "decision_ref": "ADR-022/D2",
        "note": "no ADR ever stood behind it; it entered with the auditor's supply-chain stream of 24 Jul 2026. With one principal it cannot be met in substance, and a formally-met control enters a signed artefact as independence that did not exist. Replaced by a two-key, two-device control described in numbers, not in a name."
      },
      {
        "seq": 19,
        "id": "readme-outside-tree-digest",
        "event": "DEBT_CLOSED",
        "closed_in": "v0.6.9",
        "evidence_ref": "tests/unit/test_readme_ownership.py::R-OWN-3",
        "note": "ADR-022/D12. README was the one exclusion in the tree bound nowhere else, which is the shape D11 forbids. The three generated blocks moved to docs/status_badge.md, which IS bound; the build no longer writes into a file it also hashes, so README joined the subject tree. The reason was removed rather than the symptom: a normalised hash over README was the alternative and trades byte exactness for exactness by agreement."
      },
      {
        "seq": 20,
        "id": "file-modes-and-symlink-targets",
        "event": "DEBT_CLOSED",
        "closed_in": "v0.6.9",
        "evidence_ref": "tests/unit/test_source_tree_digest.py::TREE-2 · TREE-3",
        "note": "ADR-022/D11. jjdai.source-tree/v2 carries the executable bit explicitly and records a symlink by its TARGET without dereferencing it, with the executable byte forced to zero because lstat reports the link's own bits. Two trees differing only in mode, or only in where a link points, now differ in digest."
      },
      {
        "seq": 21,
        "id": "release-revocation-by-name",
        "event": "DEBT_OPENED",
        "opened_in": "v0.6.9",
        "blocks": "meaning-of-a-release-tag",
        "note": "ADR-022 D6 requires that a release be revocable INDIVIDUALLY when the moment of a key's compromise is unknown — voiding the whole history of the key would take down releases that were sound. Implementing it needs a RELEASE_REVOKED record kind, and window 6 of the pre-genesis reserve does not contain one. Adding a serialized value to a frozen reserve is an ADR decision, not a build's to take, so this is owed rather than improvised. An earlier cut of jjdai/release.py answered it with a function that looked for a field nothing writes; that function is removed and this position replaces it. The boundary was OMITTED when this position was opened in recut1, at a time when nothing read the field; recut2 makes the gate read it, so the omission would have been a position blocking nothing."
      },
      {
        "seq": 22,
        "id": "release-key-lifecycle-witnessing",
        "event": "DEBT_OPENED",
        "opened_in": "v0.6.9-recut2",
        "blocks": "publication",
        "note": "ADR-022 D6 wants a key's activation and revocation ANCHORED in the chain, so that a registry cannot move a key's activation earlier by editing a digit. Doing it needs KEY_ACTIVATED and KEY_REVOKED record kinds, which window 6 of the pre-genesis reserve does not hold — the same wall as release-revocation-by-name, and the same answer: owed, not improvised. What the tree does today is checked and named: positions must RESOLVE to an existing index, and docs/release_keys.json declares lifecycle_binding: asserted. recut1 claimed more than that — check_lifecycle_witnessed promised in its docstring to read the record's pre-image, threw the record away, and was called by no production path, while REL-16 appended six INFER records and read one of them as the activation of a key. recut3 moves the boundary from the tag to PUBLICATION, which is strictly stronger and closes what the recut2 audit found: a RELEASE_ATTESTED record built on editable lifecycle positions was already usable as the authorization time of an L2 mutation while the tag itself was refused. No publication, no position to authorize at."
      },
      {
        "seq": 23,
        "id": "build-egress-enforcement",
        "event": "DEBT_OPENED",
        "opened_in": "v0.6.9-recut2",
        "blocks": "meaning-of-a-release-tag",
        "note": "ADR-022 D3 requires the build to run with the network DISABLED and checked. A process cannot deny its own egress: everything reachable from inside Python is a probe, and a probe answers about the endpoints it names. recut1 probed exactly 1.1.1.1:53 and pypi.org:443 and recorded 'disabled-and-checked' on a host that blocked those two and reached everything else. recut2 widens the probe and stops the overclaim — the environment table now carries network_enforcement, which is 'asserted' unless the host proves otherwise — but real enforcement is a namespace, a firewall or an air gap, which is the ceremony's to supply and the build's to record."
      },
      {
        "seq": 24,
        "id": "toolset-module-path-toctou",
        "event": "DEBT_OPENED",
        "opened_in": "v0.6.9-recut3",
        "blocks": "phase-0-gate",
        "note": "deploy/wasm-toolset/README.md said this was 'recorded as debt' and the canonical ledger held no such position — a claim about a register, made in prose, that the register did not carry. The gap itself is real: the loader hashes the module's real bytes and then hands wasmtime a PATH, and the CLI seam offers no way to pass the bytes it just verified. Closing it needs either an embedding API or a materialisation directory the runtime cannot reach between the two steps; both are Ф0 gate work on a target host, not a tree change."
      },
      {
        "seq": 25,
        "id": "release-key-lifecycle-witnessing",
        "event": "DEBT_CLOSED",
        "closed_in": "v0.6.9-recut6",
        "evidence_ref": "tests/unit/test_release_chain.py::test_key_lifecycle_positions_resolve_in_the_chain",
        "note": "ADR-022 rev 2.4 reserved KEY_ACTIVATED and KEY_REVOKED before first emission. check_lifecycle_witnessed now resolves every registry position to a record of the declared kind whose semantic_digest is key_lifecycle_digest(key_id, transition, public); a wrong kind, another key's record or the other transition of the same key refuse. docs/release_keys.json declares lifecycle_binding: witnessed and load_registry refuses anything else. The publication boundary this position held is released by this event, not by a ceremony — it was a software debt and it is paid in software."
      },
      {
        "seq": 26,
        "id": "release-revocation-by-name",
        "event": "DEBT_CLOSED",
        "closed_in": "v0.6.9-recut6",
        "evidence_ref": "tests/unit/test_release_chain.py::test_a_release_is_revoked_by_name",
        "note": "ADR-022 rev 2.4 reserved RELEASE_REVOKED, the schema jjdai.release-revocation/v1 and the signing domain JJDAI:RELEASE:REVOKE:v1. revoke_release() writes a signed RevocationStatement under a registered key valid at that position and a RELEASE_REVOKED record binding the release by statement hash; verify_publication, check_release_tag and toolset_authorizer refuse a revoked release as REL_REVOKED, and a revocation whose body cannot be read or verified refuses as REL_REVOKED_UNRESOLVED rather than lifting. The attestation stays intact: a revocation is a later signed fact about a release, not an edit of it."
      }
    ]
  },
  "drop_plan": {
    "schema": "jjdai.drop-plan/v1",
    "basis": "ADR-022/D0 · owner decision 26 Aug 2026 · Roadmap r6.9.x",
    "note": "Machine registry of deliverable -> drop. THIS is the source of truth for drop order; the roadmap's drop table and every ADR's Drop: line are checked against it by SYNC-7. Before this registry existed the order lived in prose in three documents, and ADR-020 spent a day claiming a drop the roadmap gave to the other deliverable.",
    "drops": {
      "v0.6.9": {
        "deliverable": "provenance",
        "title": "T-TOOLSET + supply chain",
        "adr": "ADR-022",
        "deadline": "2026-09-16",
        "fallback": "revert to r6.8.5 order; carry the shortfall as undelivered content"
      },
      "v0.6.10": {
        "deliverable": "alpha",
        "title": "Agent Alpha",
        "adr": "ADR-020"
      },
      "v0.6.11": {
        "deliverable": "evidence-catalogue",
        "title": "evidence-ID catalogue",
        "adr": null
      }
    },
    "keywords": {
      "provenance": [
        "T-TOOLSET",
        "supply chain",
        "тулсет",
        "ReleaseStatement",
        "происхожден"
      ],
      "alpha": [
        "Agent Alpha",
        "JaiGuruDev",
        "G-GUI",
        "GuardianBinding",
        "Alpha Harness"
      ],
      "evidence-catalogue": [
        "evidence-ID",
        "каталог evidence"
      ]
    }
  }
}
